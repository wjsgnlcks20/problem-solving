#include <bits/stdc++.h>
#define MAX 200001

using namespace std;

typedef long long ll;

int main(void){
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

    ll n, q, arr[MAX];
    cin >> n >> q;
    for(int i = 0; i < n; i++){
        cin >> arr[i];
    }
    int l, r;
    for(int i = 0; i < q; i++){
        cin >> l >> r;
        
    }


    
    return 0;
}